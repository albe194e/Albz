version: "2"
sql:
  - engine: "sqlite"
    schema:
      - "schema.sql"
    queries:
      - "queries"
    gen:
      go:
        package: "sql"
        out: "sql"
        emit_json_tags: true
        emit_db_tags: true
        emit_empty_slices: true
